#include<bits/stdc++.h>
using namespace std;
void subset(int i,vector<int>v,vector<vector<int>>ans,vector<int>num)
{
 
}
int main()
{
    vector<int>v,num;
    vector<vector<int>>ans;
    int n=3;
    while(n--)
    v.push_back(n);
    subset(0,v,ans,num);
    return 0;
}